Feature: End to End Flow of Test Scenario

    Scenario: Admin create a non product based Promotion

    Scenario: Admin create platform user and platform user edit login and edit their profile

    Scenario: Admin create user approval and status, user registered to the platform, admin approves the user, platform user login

    Scenario: Admin create org, hierarchy, user, product, view and create product based promotion

    Scenario: Admin updates the user schema, updates the user upload workflow, create a upload template, upload the file, Verify the job logs, ensure the uplaod is completed

    Scenario: Admin updates