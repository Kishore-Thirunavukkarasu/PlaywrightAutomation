Feature: Mobile Force Update

    Scenario: Verify force update configuration on Admin Site for Android

    Scenario: Verify force update configuration on Admin Site for iOS

    Scenario: Verify the updated app version is configured on admin site, user should get the force update popup on Android

    Scenario: Verify the updated app version is configured on admin site, user should get the force update popup on iOS

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, user should not be able to use the app without updating on Android

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, user should not be able to use the app without updating on iOS

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, user should be redirected to the app store to update the app on Android

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, user should be redirected to the app store to update the app on iOS

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user updates the app to the latest version, user should be able to access the app normally on Android

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user updates the app to the latest version, user should be able to access the app normally on iOS

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user tries to bypass the popup, user should be logged out from accessing the app and cannot use the app without updating on Android

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user tries to bypass the popup, user should be logged out from accessing the app and cannot use the app without updating on iOS

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user ignores the popup, user should be logged out from accessing the app and cannot use the app without updating on Android

    Scenario: Verify the updated app version is configured and user gets the popup on app launch, when user ignores the popup, user should be logged out from accessing the app and cannot use the app without updating on iOS

    Scenario: Verify no force update popup is shown when user has the latest version of the app installed on Android

    Scenario: Verify no force update popup is shown when user has the latest version of the app installed on iOS

    Scenario: Verify no force update popup is shown when user has a latest version of the app installed when admin site is configured for version below the latest version on Android

    Scenario: Verify no force update popup is shown when user has a latest version of the app installed when admin site is configured for version below the latest version on iOS

    Scenario: Verify the force update popup is shown when user has a version below the force update version configured on admin site on Android

    Scenario: Verify the force update popup is shown when user has a version below the force update version configured on admin site on iOS

    Scenario: Verify force update behavior when device is offline on Android

    Scenario: Verify force update behavior when device is offline on iOS

    Scenario: Verify force update check retry mechanism on network failure on Android

    Scenario: Verify force update check retry mechanism on network failure on iOS

    Scenario: Verify force update popup localization based on device language settings on Android

    Scenario: Verify force update popup localization based on device language settings on iOS